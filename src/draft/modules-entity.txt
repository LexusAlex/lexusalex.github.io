Проектирование предметной области
Менее связанный код

Authentication - процесс проверки подлинности пользователя
Authorization - проверка на выполнение каких-то действий на сайте
Валидация - это процесс проверки данных на соответствие неким, заранее известным правилам (формату, требованиям).

https://github.com/LexusAlex/old.lexusalex.blog/blob/master/archive/posts_old/php-project-structure.md

Code-First

А здесь мы сначала пишем код ядра, и только потом привязываем БД:

    Продумываем бизнес-логику;
    При желании пишем unit-тесты;
    Программируем сущности и сервисы;
    Привязываем базу данных.

Плюсы:

    Идеален для индивидуальной разработки;
    При написании классов не думаем о БД вообще;
    Чистый ООП без костылей;
    Использование любых типов полей;
    Получаем рабочий код без БД;
    Возможность незаметного изменения структуры БД;

Именно наличие идентификатора делает любой объект сущностью.

Агрегат в php

$employee = new Employee(
    new Id(25),
    new \DateTimeImmutable(),
    new Name('Пупкин', 'Василий', 'Петрович'),
    new Address('Россия', 'Липецкая обл.', 'г. Пушкин', 'ул. Ленина', 25),
    [
        new Phone(7, '920', '00000001'),
        new Phone(7, '910', '00000002'),
    ]
);

https://elisdn.ru/blog/104/domain-entities-modelling
https://elisdn.ru/blog/105/services-and-controllers
https://elisdn.ru/blog/106/domain-native-repository
https://elisdn.ru/blog/108/domain-entities-doctrine
https://elisdn.ru/blog/109/domain-entities-active-record
Для решения этих вопросов удобнее отказаться от автоинрементного числового идентификатора и перейти на использование уникальных рандомных идентификаторов. Но простой uniqid() не подойдёт, так как он короткий и на миллионах записей будет очень много повторов. Поэтому лучше использовать GUID – продвинутый алгоритм генерации всемирно уникальных строковых идентификаторов.

Команды - методы которые что-то модифицируют PUT POST PUTCH ок или ошибка
Запросы - просто которые чтщ считывют GET возарвщают результат
https://deworker.pro/edu/series/interactive-site/auth-use-cases
Операции которые мы может делать